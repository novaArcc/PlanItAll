import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

imageContainer: {
    width: "100%",
    height: 200,
},
image: {
    width: "100%",
    height: "100%"
}

})

export default styles